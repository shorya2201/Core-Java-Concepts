package MapsANDgenerics;

import java.util.*;

public class MapsBasic {

    /*
     * Maps store information in KEY-VALUE pair.A Map contains unique KEYS.
     * VALUE = need not to be unique.
     * KEY = must & should be unique.
     * 
     * The combination of KEY & VALUE together is known as an ENTRY.
     * 
     * Map is useful if you have to search , update or delete elements on the basis
     * of a key.
     * 
     * KEY = can be of different data types . i.e. int , string , character , etc.
     * VALUE =
     * 
     */

    /*
     * When we want to store data in a KEY-VALUE pair , we use Maps.
     * Maps allow us to store information in a KEY-VALUE pair
     * Due to this we can store duplicate VALUE and perform different operations on
     * those duplicate VALUE due to the presence of KEY.
     * Associate KEY with every VALUE.
     * Different KEYS for all VALUES.
     */

    public static void main(String[] args) {

        // Major Classes of Map Hierarchy
        HashMap hm = new HashMap();
        Hashtable ht = new Hashtable();
        LinkedHashMap lhm = new LinkedHashMap();
        TreeMap tm = new TreeMap();

    }
}
